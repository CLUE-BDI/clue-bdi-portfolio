[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "pytest",
    "httpx",
    "email-validator",
    "sqlalchemy",
    "psycopg2-binary",
    "passlib[bcrypt]",
]
